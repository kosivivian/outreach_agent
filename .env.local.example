# Web app (Vercel) environment
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...      # server-only (API routes); never exposed to the browser
WORKER_URL=http://localhost:8080   # Railway URL in production
WORKER_SECRET=change-me            # same value as the worker's WORKER_SECRET
NEXT_PUBLIC_APP_URL=http://localhost:3000
